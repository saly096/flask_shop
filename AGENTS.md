# OpenCode 规则

## 语言设置

始终使用中文（简体中文）回复用户。

## 项目规范

- 模型放在 `app/models/` 目录
- 路由放在 `app/routes/` 目录
- 工具函数放在 `app/utils/` 目录
- 测试放在 `tests/` 目录

## 技术栈

- Flask 3.0 + SQLAlchemy
- Flask-JWT-Extended 认证
- RESTful API 设计
- 蓝图组织路由

## 代码规范

- 使用统一响应格式
- 错误处理标准化
- 数据库模型继承 db.Model